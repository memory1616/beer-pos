fix: new sales appear instantly in history with pagination awareness

- Add allSales as primary data source for sales history (mirrors purchases.js pattern)
- Add historyCurrentPage variable to track current page independently
- loadSalesHistory now MERGES API data into allSales instead of replacing it,
  preserving optimistic inserts (deduplicates by id)
- Add renderHistoryPage() that renders from allSales without API call — used after
  optimistic insert and for pagination navigation
- applyOptimistic inserts into allSales + calls renderHistoryPage() when on page 1
- onSuccess removes temp, inserts real sale into allSales, calls renderHistoryPage()
- deleteSale maintains allSales and calls renderHistoryPage() in all callbacks
- Remove redundant loadData() calls — renderHistoryPage() keeps DOM in sync
