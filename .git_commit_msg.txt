fix: ensure UI updates after sale create/delete

- submitSale(): after success, call loadData() to reload all data fresh
  (customers, products, kegs, stats, sales history) instead of individual fetches
- deleteSale(): after success, call loadData() and use showToast() instead of alert()
- loadData() in sales.html: add { cache: 'no-store' } to always fetch fresh data
- dashboard.html: add { cache: 'no-store' } to /dashboard/data fetch
- remove redundant individual fetch calls from submitSale() since loadData() covers everything
