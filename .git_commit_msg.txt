fix: sales history not showing new sale after creation

- Build complete sale object in submitSale onSuccess using API response + optimistic data
  (API only returns { success, id, total, profit }; missing customer_name, date, items_qty, type, status)
- Fix double-increment of pagination total by moving increment entirely to onSuccess
- Show toast and force page 1 when not on page 1 to ensure new sale is visible
- Add try/catch fallback to loadSalesHistory() if renderSaleItem fails
- Fix submitReplacement to reload history instead of referencing non-existent data.sale
