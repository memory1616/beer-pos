fix: invoice modal refreshes after keg updates

- Add _openInvoiceSaleId tracking variable to know which invoice is open
- showInvoiceModal() saves the saleId when opened, reset when closed
- Add refreshInvoiceIfOpen(saleId) helper to re-fetch and re-render invoice
- saveKegUpdate() calls refreshInvoiceIfOpen after successful update
- submitCollectKeg() calls refreshInvoiceIfOpen after successful update
